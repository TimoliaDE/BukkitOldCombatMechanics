name: OldCombatMechanics
main: kernitus.plugin.OldCombatMechanics.OCMMain
version: 2.5.1-2026_08_02
api-version: 1.19

dependencies:
  server:
    ProtocolLib:
      required: false
      load: BEFORE
    ViaVersion:
      required: false
      load: BEFORE
    PlaceholderAPI:
      required: false
      load: BEFORE
